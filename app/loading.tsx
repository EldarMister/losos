export default function Loading() {
  return <main className="site-fallback" aria-live="polite">
    <div className="site-fallback-card">
      <span className="site-fallback-mark">МЛ</span>
      <b>Загружаем меню…</b>
    </div>
  </main>;
}
